{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Default",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "--preset",
                "default"
            ],
            "options": {
                "cwd": "${workspaceFolder}/Program" 
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
